<script src="//maps.google.com/maps/api/js?v=3.13&amp;sensor=false&amp;libraries=geometry" type="text/javascript"></script>
<script src='//google-maps-utility-library-v3.googlecode.com/svn/tags/markerclustererplus/2.0.14/src/markerclusterer_packed.js' type='text/javascript'></script>

<% places = [] %>
<%= form_tag(itineraries_path) do %>
  <ol id="each_activity">
    <% i = 0 %>
    <% @activities.each do |activity| %>
      <li><%= activity[i].name %>, <%= activity[i].location.city %>, <%= activity[i].rating %>, <%= link_to 'Replace', remote: true %><br><br></li>
        <%= hidden_field_tag "activity_ids[]", activity[i].id %>
        <% places << activity[i] %>
      <% i += 1 %>
      <% i = 0 if i == 10 %>
    <% end %>
  </ol>
  <%# if user_signed_in? %>
  <%= submit_tag "Save Itinerary" %>
  <%# else %>
   <%#You need to log in>
  <%# end %>
<% end %>

<% @hash = Gmaps4rails.build_markers(places) do |a, marker|
            marker.lat a.location.coordinate.latitude
            marker.lng a.location.coordinate.longitude
          end %>

<div style='width: 800px;'>
  <div id="map" style='width: 800px; height: 400px;'></div>
</div>
<script type="text/javascript">
  handler = Gmaps.build('Google');
  handler.buildMap({
      provider: {
      },
      internal: {
        id: 'map'
      }
    },
  function(){
      markers = handler.addMarkers(<%=raw @hash.to_json %>);
      handler.bounds.extendWith(markers);
      handler.fitMapToBounds();
    }
  );

</script>
